

services:
  catalogue:
    build:
      context: .
      dockerfile: Dockerfile
    image: catalogue:v1
    container_name: catalogue
    depends_on:
    - mongodb
  mongodb:
   image: mongodb:v1
     

networks:
  default:
    driver: bridge
    name: roboshop
    external: true
      